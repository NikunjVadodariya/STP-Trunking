server:
  host: "0.0.0.0"
  port: 5060
  domain: "localhost"
  realm: "localhost"

database:
  url: "sqlite:///./sip_trunking.db"
  echo: false

api:
  host: "0.0.0.0"
  port: 8000
  cors_origins: ["*"]
  secret_key: "your-secret-key-change-in-production"

logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"

